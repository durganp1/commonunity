

<article>
    <a href = '/yourpage'> &larr; Back to Your Page</a>
    <h2>
        Edit Post
    </h2>
    <form class = 'edit-post-form'>
        <div>
            <input name = 'post-title' type = 'text' value = '{{post.title}}' />
            <span>{{{post.post_message}}}</span>
        </div>
        <div>
            {{post.like_count}} {{format_plural 'like' post.like_count}} by you on {{format_date post.created_at}}
            |
            <a href = '/post/{{post.id}}'>{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
        </div>
        <button type = 'submit'>Save Post</button>
        <button type = 'button' class = 'delete-post-button'>Delete Post</button>
    </form>
</article>

<form class = 'comment-form'>
    <div>
        <textarea name = 'comment-body'></textarea>
    </div>
    <div>
        <button type = 'submit'>Add comment</button>
    </div>
</form>

{{> comments post.comments}}

<script src = '/javascript/comment.js'></script>

